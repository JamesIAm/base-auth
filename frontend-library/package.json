{
	"name": "base-auth-client",
	"version": "1.0.0",
	"description": "A client to assist in the use of the base auth backend",
	"main": "index.js",
	"types": "index.d.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"compile": "pnpm tsc && cp package.json ./dist",
		"package": "cd dist && pnpm pack"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/JamesIAm/base-auth.git"
	},
	"author": "James Nahajski",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/JamesIAm/base-auth/issues"
	},
	"homepage": "https://github.com/JamesIAm/base-auth#readme",
	"devDependencies": {
		"typescript": "^5.5.3"
	}
}
